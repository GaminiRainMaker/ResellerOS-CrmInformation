'use client';
import Typography from '@/app/components/common/typography';
import React from 'react';

const Salesforce = () => {
  return (
    <div
      style={{
        display: 'flex',
        justifyContent: 'center',
        alignItems: 'center',
        height: '100vh',
        backgroundColor: 'white',
      }}
    >
      <Typography name="Body 2/Bold">Salesforce Canvas Loading...</Typography>
    </div>
  );
};

export default Salesforce;
