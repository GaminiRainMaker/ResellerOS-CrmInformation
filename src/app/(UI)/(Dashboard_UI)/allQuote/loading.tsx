import PageSkeleton from '@/app/components/common/os-skeletons/PageSkeleton';

export default function Loading() {
  // You can add any UI inside Loading, including a Skeleton.
  return <PageSkeleton />;
}
