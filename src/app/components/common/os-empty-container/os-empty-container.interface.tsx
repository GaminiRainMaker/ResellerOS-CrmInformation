import {ReactElement} from 'react';

export interface EmptyContainerInterface {
  title?: string;
  subTitle?: string;
  actionButton?: string;
  onClick?: any;
  MetricsIcon?: boolean;
  buttonContainer?: ReactElement;
}
